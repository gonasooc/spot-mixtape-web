# spot-mixtape-web 디자인 기준

## 목적

랜딩은 앱이 무엇인지 한 화면에서 전달하고 출시 소식 구독으로 이어준다. 법률 문서는
스토어 심사관과 이용자가 장식 없이 빠르게 읽는 문서다. 두 표면의 성격이 다르므로
껍데기(헤더·푸터)만 공유하고 본문 배경을 나눈다.

## 방법

장식을 덜어내고 구조로 읽히게 한다. 토큰 계약 하나, 여백이 만드는 섹션 리듬, 분리선
대신 표면 차이, 굵기 제한, 밑줄 없는 solid 액션, 납작한 헤더와 푸터가 그 방법이다.
스크롤 등장도 거리와 시간을 정해두고 그 안에서만 쓴다.

색과 서체는 `PRODUCT.md`의 digital vinyl과 앱 토큰을 그대로 둔다. 사이트가 앱보다 앞에
나서지 않는다.

문구는 `humanize-korean`의 quick-rules(번역투·AI 관용구·기계적 대구·과장 수사)로
점검했다. 원문: <https://github.com/epoko77-ai/im-not-ai>.

## 시각 토큰

기준 파일은 `src/styles.css`의 `@theme` 하나다. 컴포넌트에 색·크기 임의값을 쓰지 않는다.

어두운 껍데기는 캔버스 `#0b0d0a`, 표면 `#171a15`, 조용한 컨트롤 면 `#272d1e`(hover
`#333a28`), 실선 `#2a2f25`를 쓴다. 글자는 `#f3f5ec`, 보조 글자 `#a8ae9f`, 잔글씨
`#838a7e`. 액센트는 애시드 라임 `#c9f55d` 하나뿐이다.

법률 문서는 페이퍼 `#f3f5ec` 위에 제목 `#14170e`, 본문 `#383e30`, 메타 `#5c6352`를 쓴다.
액센트는 페이퍼에서 보이지 않으므로 `#45560b`로 어둡게 잡은 값을 따로 둔다.

간격은 4·8·12·16·24·32·48·64·96·144를 쓴다. 모서리는 컨트롤 8px, 액션 14px, 카드 16px.
콘텐츠 정렬선은 1160px, 좌우 여백은 24px(모바일 20px)다.

### 토큰 이름 함정

Tailwind의 `text-*`는 색 네임스페이스와 글자 크기 네임스페이스를 함께 읽는다. 같은
이름이 양쪽에 있으면 `text-<name>`이 **색으로** 해석되고 크기 지정은 조용히 사라진다.
`--color-control`과 `--text-control`을 같이 두었다가 본문이 배경색으로 칠해지는 일이
실제로 있었고, 빌드도 런타임도 아무 경고를 내지 않았다. 색 토큰을 `--color-chip`으로
바꿔 해결했다. `pnpm tokens:check`가 이 충돌을 빌드 전에 잡는다.

## 서체

한국어 본문은 Pretendard Variable을 자체 호스팅한다. `public/_headers`의 CSP가
`font-src 'self'`라 외부 CDN은 차단되고, dynamic subset이라 페이지가 실제로 그리는
유니코드 구간만 내려받는다. 라틴 워드마크는 Outfit, 숫자와 라틴 메타 라벨은
JetBrains Mono를 쓴다. 셋 다 self-host이므로 외부 요청은 0건이다.

한글 웹폰트는 공짜가 아니다. 랜딩은 Pretendard subset 13개를 받고(405KB), 법률 문서는
글자가 많아 15개(459KB)까지 간다. `font-display: swap`이라 첫 페인트를 막지 않고
`/assets/*`가 1년 immutable 캐시라 두 번째 방문부터는 요청이 없다. 더 줄이려면 이 사이트가
실제로 쓰는 글리프만 남기는 정적 subset을 만들어야 하는데, 문구를 고칠 때마다 다시
만들어야 해서 지금은 하지 않는다. OS마다 한글 인상이 달라지는 문제를 없애는 값으로 받아들인다.

세 글꼴 모두 OFL 1.1이다. 폰트 파일을 함께 배포하므로 고지도 같이 실어야 하는데,
미니파이어가 스타일시트의 주석을 지운다. `public/licenses/fonts.txt`에 세 고지와 라이선스
전문을 두고 푸터에서 연결한다.

굵기는 400과 500만 쓴다. 제목·`strong`·`th`·`dt`까지 500으로 묶었고 700은 어디에도
없다. 위계는 크기·색·여백이 만든다.

mono에 대문자와 0.1em 자간을 얹는 `eyebrow`는 라틴과 숫자에만 쓴다. 한글을 그렇게
조판하면 다른 글꼴로 대체되면서 늘어져 보인다. 한글 소제목 라벨은 `SectionLabel`이
sans 13px으로 그린다.

## 구조와 리듬

섹션 사이는 데스크톱 144px, 모바일 88px이다. 섹션을 가르는 것은 이 여백과 표면 차이
(canvas ↔ surface)이며 가로선을 긋지 않는다. 표 테두리처럼 구조를 설명하는 선만 남긴다.

히어로는 헤더를 뺀 한 화면 높이를 최소로 잡고 내용을 세로 가운데에 둔다. 카피와 CTA는
중앙 정렬, 그 아래 사운드 카드를 둔다. 카드는 회전·광원·그라데이션 없이 표면과 여백으로
서 있다.

헤더는 60px(모바일 52px), 워드마크와 고스트 내비만 둔다. 활성 항목은 밑줄 막대가 아니라
컨트롤 면으로 표시한다. 한글 메뉴 이름이 320px 헤더보다 길어서 내비만 가로 스크롤한다.
문서 전체가 넘치지는 않으며, 두 법률 문서는 푸터와 랜딩에서도 닿을 수 있다.

푸터는 한 줄이다. 앱 이름, 두 문서 링크, 운영자와 시행일까지만 둔다. 테두리를 두지 않고
위쪽 여백으로 분리한다.

법률 문서는 한 열(736px)에 제목·시행일·목차·본문만 놓는다. 히어로도 등장 모션도 없다.

## 액션과 링크

액션은 모두 밑줄 없는 solid 면이다. 높이 52px(작은 변형 44px), 좌우 22px, 모서리 14px,
글자 15px에 굵기 500. 누를 때 1px 내려앉는 것 말고 hover에서 떠오르지 않는다.

기본은 애시드 면에 캔버스 글자, 보조는 컨트롤 면에 밝은 글자다. 페이퍼 위에는 잉크 면과
페이퍼 글자를 쓴다. 밑줄은 문장 안에 섞인 링크에만 남긴다.

포커스 링은 2px 애시드에 4px 오프셋이며, 페이퍼 위에서는 잉크색으로 바뀐다.

## 모션

히어로는 CSS 애니메이션으로 60ms 간격, 620ms 동안 12px 아래에서 올라온다. JavaScript가
없어도 그대로 보이고, 프리렌더된 마크업에 기대지 않는다.

그 아래는 `src/components/Reveal.tsx`가 맡는다. 텍스트는 18px 이동에 600ms, 그래픽은
28px 이동과 0.985배에 760ms, easing은 `cubic-bezier(.22, 1, .36, 1)`이다. 같은 묶음 안
간격은 80ms이고 160ms에서 멈춘다. 화면 하단보다 48px 안쪽에 닿으면 한 번만 재생한다.

숨기는 상태는 이 훅만 만든다. 첫 페인트에 이미 화면에 있는 요소는 건드리지 않는다 —
보이던 것을 감췄다가 다시 띄우면 등장이 아니라 깜박임이다. 부모와 자식에 함께 걸지
않는다. Tab이 아직 숨은 요소에 먼저 닿으면 그 자리에서 드러낸다. `prefers-reduced-motion`
에서는 훅이 아무것도 하지 않고 CSS가 한 번 더 막는다.

블러와 스크롤 가로채기는 쓰지 않는다.

## 한국어 문구

`word-break: keep-all`로 어절 안에서 줄이 끊기지 않게 한다. 제목은 `text-wrap: balance`,
본문은 `pretty`를 쓴다.

랜딩의 모든 주장은 앱 코드베이스와 일치해야 한다. 10초 제한, 비공개 저장, 서명 링크,
삭제 범위는 실제 동작이며 문구를 고칠 때 함께 확인한다.

문구를 고칠 때 점검하는 것: 번역투(`~를 통해`, `~에 있어`, `~을 가지고 있다`, 이중 피동),
AI 결산 관용구(`결론적으로`, `~하는 이유다`, `필요한 것은 ~이다`), 같은 종결어미 연속,
`A가 아니라 B` 대구의 반복, 과장 수사, 이모지. 대구는 랜딩에 한 번만 쓴다
(`빠진 기능이 아니라, 내린 결정입니다`).

## 하지 않는 것

테마 토글을 넣지 않는다. 앱이 다크 기준이므로 사이트도 고정한다.

배경 그라데이션, 장식용 그림자, 광원 블롭, 섹션 분리선, 이모지, 텍스트 화살표를 쓰지
않는다. 필름 그레인 한 겹만 예외이며 앱의 촉각을 옮긴 브랜드 요소다.

가짜 지표, 후기, 호환성 로고를 만들지 않는다. 굵기 600 이상과 임의 색값도 쓰지 않는다.

## 검증

`pnpm build`가 `tokens:check`를 먼저 돌리고 타입 검사, 클라이언트·SSR 빌드, 프리렌더를
이어서 실행한다. `pnpm config:check`는 법률 문서의 플레이스홀더를 따로 본다.

헤드리스 Chrome(CDP)으로 측정한 결과다. 실제 기기·실제 스크린리더 검증과 구분한다.

- 320 / 390 / 768 / 1280px에서 4개 페이지 모두 가로 넘침 0건.
- 렌더된 글자 중 굵기 500 초과 0건.
- 본문·라벨의 색과 배경 대비가 모두 WCAG AA(작은 글자 4.5:1) 이상. 잔글씨
  `#767c70`이 표면 위에서 4.09:1이라 `#838a7e`로 올렸다.
- 프리렌더된 HTML에 `data-reveal`이 없고 본문 전체가 들어 있다. JavaScript 없이 읽힌다.
- `prefers-reduced-motion: reduce`에서 숨겨진 요소 0건이고, 파형 막대의 반복 애니메이션도
  멈춘다. 시간만 0으로 줄이면 마지막 키프레임인 45% 높이에서 굳으므로 따로 끈다.
- 외부 네트워크 요청 0건. 랜딩 18요청·516KB, 개인정보처리방침 20요청·573KB(캐시 없는 첫 방문).

측정하지 않은 것: 실제 브라우저 수동 조작, VoiceOver·TalkBack, 실기기 스크롤, Lighthouse,
인쇄 출력.
